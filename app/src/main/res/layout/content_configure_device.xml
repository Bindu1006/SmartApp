<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/content_configure_device"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:background="@color/background"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    app:layout_behavior="@string/appbar_scrolling_view_behavior"
    tools:context="com.spectrum.smartapp.StartUpConfigurePiActivity"
    tools:showIn="@layout/app_bar_configure_device">

    <ProgressBar
        android:id="@+id/deviceProgressBar"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:indeterminate="true"
        android:visibility="gone"
        style="@style/Widget.AppCompat.ProgressBar.Horizontal"/>

    <Button
        android:layout_width="200dp"
        android:layout_height="40dp"
        android:id="@+id/configureDevice"
        android:text="Configure"
        android:background="@drawable/buttonlayout"
        android:layout_marginTop="50dp"
        android:layout_centerHorizontal="true" />

    <ListView
        android:id="@+id/output"
        android:layout_below="@+id/configureDevice"
        android:layout_width="fill_parent"
        android:layout_height="fill_parent"
        android:layout_marginTop="50dp"
        android:dividerHeight="0dip"
        android:cacheColorHint="#00000000" />


</RelativeLayout>
